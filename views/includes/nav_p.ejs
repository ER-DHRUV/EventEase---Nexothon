<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>navbar</title>
    <link rel="stylesheet" href="">
</head>
<body>
    
<!-- Sidebar Overlay -->
<div class="sidebar-overlay" id="sidebarOverlay"></div>

<!-- Header -->
<header class="header">
    <div>
        <div class="logo-section">
            <div class="logo"></div>
            <div class="logo-text">EventEase</div>
        </div>
        <div class="tagline">Smart Event Booking & Management with AI Recommendations</div>
    </div>

    <div class="profile-section">
        <div class="profile-icon" title="Profile">👤</div>
        <h4 class="username">
            <%= user.name %>
        </h4>
    </div>
    <div class="logout-modal-overlay" id="logoutOverlay">
        <div class="logout-modal">
            <p>Are you sure you want to logout?</p>
            <a href="/logout" class="logout-link">Logout</a>
        </div>
    </div>


</header>

<!-- Navigation -->
<nav class="nav-bar">
    <div class="nav-left">
        <button class="nav-menu" id="nav-menu">
            ☰ Menu
        </button>
        <ul class="nav-links" id="navLinks">
            <% if (user && user.category==="organizer" ) { %>
                <li><a href="/welcome_organizer" class="active">Home</a></li>
                <li><a href="/history_organizer">History</a></li>
                <li><a href="#">Tickets</a></li>
                <li><a href="/createEvent">Create Event</a></li>
                <% } %>
                    <% if (user && user.category==="public" ) { %>
                        <li><a href="/welcome_public" class="active">Home</a></li>
                        <li><a href="#">History</a></li>
                        <li><a href="#">Tickets</a></li>
                        <% } %>


        </ul>
    </div>
    <button class="menu-toggle" id="menuToggle">
        🔍 Filters
    </button>
</nav>
    
</body>
</html>

